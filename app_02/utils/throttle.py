class FrequenceThrottle(object):
    """
    Throttle is the same as permission and authentication
    """
    def allow_request(self, request, view_instance):
        if i:
            return True
        else:
            return False
